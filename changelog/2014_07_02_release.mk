Faster builds

* We improved the time Snap takes to setup your build, so it will now take less time to start running.
* Build history page now shows *scheduled* and *cloning*, so you have a better sense of what is going on at the first moments of each pipeline stage.

An all brand new Pipeline Configuration page

* Cleaner interface.
* Intuitive edition.
* Takes less clicks to configure a pipeline.