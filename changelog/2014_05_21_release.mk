We're now building support for Node.js!

* You can now go to your project configuration page and add your favorite node version to the build.
* We continue to build support for Node.js. We are currently building detection for Node, so that setting up a Node project
becomes easier. If there is something you would like to see in Node JS projects, we would like to <a href="/contact-us">hear from you</a>.

We've pushed some bug fixes that prevented you from adding a trailing slash to your artifacts.

The build history page now has the favicon that shows you the state of the last pipeline.
