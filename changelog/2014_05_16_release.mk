This time around we're just fixing bugs!

* Fixed some bugs with "Rebuild Last Revision" building the oldest commit in the latest pipeline instead of the newest commit.
* Fixed some bugs withediting a build plan to add multiple stages at a time.
